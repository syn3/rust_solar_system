[package]
name = "solar_system"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

[dev-dependencies]
macroquad = "0.4"
